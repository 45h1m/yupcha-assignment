{
    "name": "yupcha-assignment",
    "version": "1.0.0",
    "scripts": {
      "build": "npm run build:server && npm run build:frontend",
      "build:server": "cd server && npm install",
      "build:frontend": "cd frontend && npm install && npm run build",
      "start": "node ./server/src/server.js"
    }
  }